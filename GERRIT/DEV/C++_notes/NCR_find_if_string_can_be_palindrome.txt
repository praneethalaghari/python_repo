/******************************************************************************

                              Online C++ Compiler.
               Code, Compile, Run and Debug C++ program online.
Write your code in this editor and press "Run" button to compile and execute it.

*******************************************************************************/

#include <iostream>
#include <cstring>
#include <map>

using namespace std;

int main()
{
    char *s = "abcbafeeecfgghh";
    
    int length = 0;
    for(int i=0;s[i]!='\0';i++)
    {
        length++;
    }
   
    cout << length << endl;
   
    char p[length];
    strcpy(p,s);
    
    for(int j=0;j<strlen(p);j++)
    {
        cout << p[j] << endl;
    }
    
    map<char,int> m1;
    
    for(char x:p)
    {
        map<char,int>::iterator it1;
        it1=m1.find(x);
        
        if(it1!=m1.end())
        {
            it1->second = it1->second +1;
        }
        else
        {
            m1.insert(pair<char,int>(x,1));
        }
    }
    
    map<char,int>::iterator it;
    int flag = 0;
            
    for(it=m1.begin();it!=m1.end();it++)
    {
        cout << it->first << " " << it->second << endl;
        
        if (m1.size() & 1)
        {
            if(flag)
            {
             if(it->second & 1)
              {
                cout << "Cant make palindrome" << endl;
                break;
              }   
            }
            if(it->second & 1)
            {
                flag = 1;
            }
        }
        else
        {
             if(it->second & 1)
              {
                cout << "Cant make palindrome" << endl;
                break;
              }
        }
        
    }

    return 0;
}
